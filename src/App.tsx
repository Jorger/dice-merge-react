import React from "react";
import AppWrapper from "./components/appWrapper";

const App = () => {
  return (
    <AppWrapper>
      <div>Test</div>
    </AppWrapper>
  );
};

export default React.memo(App);
